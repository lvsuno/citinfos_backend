"""Test package for analytics app."""

# This file makes the analytics.tests directory an importable package so
# tests can be referenced via dotted module paths (eg. analytics.tests.test_cleanup_task).
